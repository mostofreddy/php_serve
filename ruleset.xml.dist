<?xml version="1.0"?>
<ruleset name="Custom PSR12">
    <description>The PSR12, but warnings at 140 chars.</description>

    <!-- Include the whole PEAR standard -->
    <rule ref="PSR12"/>

    <!-- Lines can be no longer than 140 chars -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="140"/>
        </properties>
    </rule>

    <!-- Files -->
    <file>./src</file>

    <!-- Params -->
    <arg name="colors"/>
</ruleset>
